# jack-toolkit 全套依賴（tkinter 為 Python 內建不列）
# 每支子工具皆為獨立程序、共用此 requirements。

# Serial Port Tool
pyserial>=3.5

# FT232H Tester
pyftdi>=0.55.4
pyusb>=1.2.1
libusb-package>=1.0.26.2

# USB-HID Tester（Windows wheel 自帶 hidapi.dll，走 OS 內建 HID driver）
hidapi>=0.14

# 校正設計工具 calib_designer（matplotlib 會一併帶入 numpy）
matplotlib>=3.5

# 打包工具（要產 .exe 時才需要，預設不裝）
# pyinstaller>=6.0
